import { RemoveMinusSignPipe } from './remove-minus-sign.pipe';

describe('RemoveMinusSignPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveMinusSignPipe();
    expect(pipe).toBeTruthy();
  });
});
